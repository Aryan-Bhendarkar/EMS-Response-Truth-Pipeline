# Everything in requirements.txt, plus tests and notebooks.
-r requirements.txt
# pandas: used only by tests/ and notebooks/ (DuckDB .df()), not by pipeline code.
pandas==3.0.6
pytest==9.1.1
jupyter==1.1.1
ipykernel==7.3.0
nbformat==5.11.1
