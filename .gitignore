# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# Secrets
.env

# Raw data (kept locally, not committed) — one sample month is force-added, see docs/source_map.md
data/raw/*
!data/raw/.gitkeep
!data/raw/sample_month/

# Processed data and local warehouse
data/processed/*
!data/processed/.gitkeep
*.duckdb
*.duckdb.wal

# Pipeline outputs regenerated by every run (evidence copies live in outputs/<month>/ and are committed selectively)
logs/*
!logs/.gitkeep

# OS
.DS_Store
Thumbs.db

# Course material, not a project deliverable — repo will be public
FDE_Assignment2.pdf
